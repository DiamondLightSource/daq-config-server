# show coverage in CI status, not as a comment.
comment: false
coverage:
  status:
    project:
      default:
        target: 95
        threshold: 1%
    patch:
      default:
        target: 100
        threshold: 1%
github_checks:
  annotations: false
